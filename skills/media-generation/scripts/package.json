{
  "name": "modelmax-mcp-server",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node index.mjs",
    "postinstall": "node setup.mjs && (mcporter config add modelmax-media \"node $(pwd)/index.mjs\" || echo 'mcporter auto-registration skipped')"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  }
}
